LUMEN API — Ops & Turnos
Instalar: pnpm -C apps/api install
Dev: pnpm -C apps/api run dev
Puerto: 4000 por defecto (PORT para cambiarlo).
Endpoints: GET /health y GET /meta.
Dataset demo in-memory; se reinicia al reiniciar el server.